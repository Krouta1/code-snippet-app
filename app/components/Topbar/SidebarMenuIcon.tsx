import { MenuOutlined } from "@mui/icons-material";
import React from "react";

const SidebarMenuIcon = () => {
  return (
    <MenuOutlined className="hidden cursor-pointer text-slate-500 max-md:block" />
  );
};

export default SidebarMenuIcon;
